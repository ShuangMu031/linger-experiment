# Runtime dependencies for the experiment + figure layer.
# The vendored linger_core backend may pull additional deps (see its own pyproject).
python-dotenv>=1.0
numpy>=1.24
matplotlib>=3.7
openai>=1.0
